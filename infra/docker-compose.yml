version: '3.8'

services:
  postgres:
    image: postgres:15-alpine
    container_name: elephantfly-postgres
    environment:
      POSTGRES_USER: elephantfly
      POSTGRES_PASSWORD: elephantfly_dev
      POSTGRES_DB: elephantfly_scan
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U elephantfly"]
      interval: 10s
      timeout: 5s
      retries: 5

  adminer:
    image: adminer:latest
    container_name: elephantfly-adminer
    ports:
      - "8080:8080"
    depends_on:
      postgres:
        condition: service_healthy
    environment:
      ADMINER_DEFAULT_SERVER: postgres

volumes:
  postgres_data:

